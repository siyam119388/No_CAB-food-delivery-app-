package com.nocap.controller;

/* CustomerController — base path /api/customer
   Build in: Part 4 + Part 6

   Endpoints:
   GET  /restaurants                 approved restaurants only
   GET  /restaurants/{id}            details + hygiene score
   GET  /restaurants/{id}/menu       available menu items
   POST /orders                      place an order
   GET  /orders                      my orders
   GET  /orders/{id}                 tracking status
   POST /reviews                     submit review (delivered orders only)
   GET  /profile  |  PUT /profile
*/

// TODO: @RestController @RequestMapping("/api/customer") class
